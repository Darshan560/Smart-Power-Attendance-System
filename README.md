# Smart-Power-Attendance-System
Smart Power Attendance System is an IoT-based biometric solution using a fingerprint sensor and NodeMCU to automate attendance. It records in/out time, stores data in a MySQL database via a web interface, ensures accuracy, prevents proxy, and supports real-time updates with reports and notifications.
